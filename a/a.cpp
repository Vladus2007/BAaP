#include <iostream>
#include <vector>
#include <string>
#include <fstream>
int main(){
    int n,m;
    string s;
    cin>>s;

    ifstream inFile(s);
    inFile>>n>>m;
    vector<vector<int>> adj(n);
    

}